To run this project follow this command

goto project directory and run "npm install" 
after execution of above command run "npm start" this will start the backend server

now goto cilent directory and run "npm install"
after execution of above command run "npm start" this will start the front end server

now you can check the project on http://localhost

there is a deployed project on heroku you can check them  here:  https://devwork.herokuapp.com

there you can create a new account or just login with default login the credincials are
	username: "admin@devwork.com"
	password: "admin"

Nodejs and ExpressJs used as backend
Reactjs used for frontend
mongodb used for database
PassortJs used for login authentication 
bcryt used for password encription'
apexchart used for chart
bootstrap used as css library